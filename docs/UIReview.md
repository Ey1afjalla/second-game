# UI 自查记录

## 2026-07-14 Local Demo Reset Pass

检查范围：

1. 本地存档卡新增“重置本地演示”按钮。
2. 按钮需要和导入、导出、演示槽位处于同一存档管理区域。
3. 重置后需要给出明确反馈，说明当前自动存档被覆盖但槽位保留。
4. Agent 工作台需要能看到本地演示重置相关行动计划和调参建议。
5. 配置视图需要能看到 `resetCurrentDemo`。

自查结论：

1. 桌面浏览器验证中，“重置本地演示”位于本地存档卡内，和导出、导入、槽位管理归为同一区域。
2. 保存槽位后点击重置，页面回到干净 Day 1，事件恢复为 2 条，并显示“已重置为干净开局，并覆盖当前自动存档；演示槽位仍然保留。”。
3. 重置后已保存槽位仍在列表中，可继续读取或删除。
4. Agent 页已能看到“重置本地演示开局”和 `local_demo_reset`。
5. 配置视图已能看到 `resetCurrentDemo`、`saveSlots` 和 `importableSnapshots`。
6. 浏览器插件 viewport 覆盖未实际切换到 390px；桌面验证无横向溢出，移动端沿用现有单列布局和按钮宽度约束。

## 2026-07-14 Ops Log Export Pass

检查范围：

1. 运营诊断后台新增“运营日志导出”面板。
2. 导出按钮需要在桌面和窄屏下都清晰可点，不挤压 KPI 与图表。
3. 点击后需要给出可见反馈，便于面试现场确认操作成功。
4. Agent 工作台需要能看到运营日志相关行动计划和调参建议。
5. 配置视图需要能看到 `opsLogExport` 和 `second-game-ops-log`。

自查结论：

1. 桌面浏览器验证中，运营页首屏显示导出面板，未挤压 KPI 区域。
2. 点击“导出运营日志”后出现“已生成本地运营日志 JSON。”反馈，控制台无错误。
3. Agent 页已能看到“导出运营日志证据包”和 `ops_log_export`。
4. 配置视图已能看到 `opsLogExport` 与 `second-game-ops-log`。
5. 浏览器插件 viewport 覆盖未实际切换到 390px；样式层已确认 `max-width: 980px` 下导出面板改为纵向排列，按钮宽度为 100%。

## 2026-07-14 Multi Save Slots Pass

检查范围：

1. 游戏主界面本地存档卡新增演示槽位区域。
2. 槽位区域需要支持保存当前局面、读取槽位和删除槽位。
3. 空槽位状态需要有清晰提示，不让玩家误以为功能失败。
4. 槽位列表不能遮挡导入、导出、接待、收摊和补货等核心操作。
5. Agent 工作台和配置视图需要能说明多槽位能力。

自查结论：

1. 演示槽位区域位于导入/导出按钮下方，和本地存档归为同一张卡。
2. 槽位最多显示 3 个，信息包含经营日、成交数、保存时间、接待数和铜钱，适合面试前快速辨认局面。
3. 空状态提示玩家可以保存局面用于面试一键读取。
4. 浏览器桌面验证中，保存、读取、删除槽位均能给出可见反馈。
5. Agent 页和配置页均能看到多槽位行动计划、调参目标和 `saveSlots` 配置字段。
6. 390px 窄屏下槽位区域无横向溢出，控制台无错误。

## 2026-07-14 Snapshot Import Pass

检查范围：

1. 游戏主界面本地存档卡新增“导入演示快照”按钮。
2. 导入按钮需要和导出按钮处于同一操作组，但不能遮挡存档状态、事件数量和配置历史数量。
3. 有效快照导入后需要恢复局内状态，并给出明确反馈。
4. 无效 JSON 或版本不匹配文件需要显示错误提示，不破坏当前局面。
5. Agent 工作台和配置视图需要能说明快照导入能力。

自查结论：

1. 导入按钮使用上传图标和隐藏文件输入，视觉上与导出按钮保持同级操作。
2. 导入失败反馈复用本地存档卡的异常状态，玩家不用去控制台查原因。
3. 导入成功后会把 `lastMessage` 改成快照恢复提示，并继续触发本地自动存档。
4. 浏览器桌面验证中，导入按钮、导出按钮、本地存档卡和 JSON 文件类型限制均正常显示。
5. Agent 页和配置页均能看到快照导入相关行动计划、调参目标和 `localPersistence.imports` 配置字段。
6. 390px 窄屏下本地存档卡、导入按钮和导出按钮无横向溢出，控制台无错误。
7. 当前浏览器自动化接口不提供本地文件选择器上传能力，真实 JSON 文件选择需人工点击确认。

## 2026-07-14 Local Persistence Pass

检查范围：

1. 游戏主界面新增本地存档卡。
2. 本地存档卡需要显示保存状态、事件数量、配置历史数量和导出按钮。
3. 页面刷新后需要恢复已保存游戏状态。
4. Agent 工作台需要显示导出演示快照相关行动计划和调参建议。
5. 配置视图需要显示 `localPersistence` 配置字段。

自查结论：

1. 本地存档卡位于核心操作按钮下方，玩家完成接待或收摊后能看到保存状态。
2. “导出演示快照”按钮带图标，和接待、收摊、重开等核心操作保持区分。
3. 存档卡文案明确说明快照只保存在本机，不涉及外部上传。
4. 浏览器桌面验证中，接待后刷新页面能恢复成交后的局内状态，说明自动存档链路可用。
5. Agent 页和配置页均能看到本地快照相关行动计划、调参建议和 `localPersistence` 配置字段。
6. 390px 窄屏下本地存档卡、状态文案和导出按钮无横向溢出，控制台无错误。
7. 点击导出按钮后页面反馈正常，下载行为由浏览器 Blob 链接触发，快照内容仅限本地演示数据。

## 2026-07-14 Rumor Heat Pass

检查范围：

1. 游戏主界面新增怪谈热度卡。
2. 商品组合卡需要显示热度影响，避免风险规则藏在后台。
3. 收摊后消息需要显示怪谈热度和下一日事件倾向。
4. 运营后台需要新增怪谈热度 KPI 和热度快照信息。
5. Agent 工作台需要显示 `read_rumor_forecast`、热度证据、行动计划和调参建议。
6. 配置视图需要显示 `rumorForecast` 配置字段。

自查结论：

1. 怪谈热度卡位于商品组合和 NPC 面板之间，玩家能在接待前理解组合风险。
2. 热度卡使用深色传闻风格，高低热度有不同视觉状态，但文字保持高对比度。
3. 组合卡在效果文案中显示热度增减，风险规则不会只停留在配置层。
4. 运营后台使用 KPI 和策略面板展示热度，不破坏后台扫描效率。
5. 浏览器实测中，游戏页、运营页、Agent 页和配置页均能读取怪谈热度。
6. 390px 窄屏下怪谈热度卡无横向溢出，控制台无错误。

## 2026-07-14 Product Combo Pass

检查范围：

1. 游戏主界面新增商品组合卡。
2. 组合卡需要显示激活数量、组合名称、主题说明、缺少商品和激活效果。
3. 运营后台需要新增组合激活 KPI 和商品组合策略面板。
4. Agent 工作台需要显示组合证据、异常、行动计划、调参建议和工具轨迹。
5. 配置视图需要显示 `productCombos` 配置字段。

自查结论：

1. 商品组合卡位于声望分支和 NPC 面板之间，玩家在接待前能看到库存组合带来的实际规则。
2. 激活组合使用浅绿色状态，未激活组合显示缺少商品，和夜市暖灯主题保持一致。
3. 运营页采用紧凑策略面板展示组合激活率和触发次数，没有把游戏装饰带入后台数据区。
4. Agent 页通过 `read_product_combos` 和商品组合诊断解释当前库存策略。
5. 配置页输出 `productCombos`，便于说明组合规则可以进入 LiveOps 热更新。
6. 浏览器实测中，游戏页、运营页、Agent 页和配置页均能读取商品组合。
7. 390px 窄屏下商品组合卡无横向溢出，控制台无错误。

## 2026-07-14 Reputation Branch Pass

检查范围：

1. 游戏主界面新增声望分支卡。
2. 声望分支卡需要显示当前状态、门槛进度、分支说明和规则效果。
3. 运营后台需要显示声望分支 KPI。
4. Agent 工作台需要显示声望分支证据、异常、行动计划、调参建议和工具轨迹。
5. 配置视图需要显示 `reputationBranches` 配置字段。

自查结论：

1. 声望分支卡位于核心数值和 NPC 面板之间，玩家能在接待前看到当前声望对经营的影响。
2. “街坊观望 / 巡夜调查 / 灯影贵客”使用同一布局，不会因为状态切换造成明显跳动。
3. 运营页新增 KPI 仍保持后台扫描感，没有把游戏装饰带入数据区。
4. Agent 页可见 `读取声望分支` 和 `read_reputation_branch`，适合解释 Agent 输入。
5. 浏览器实测中，游戏页、运营页、Agent 页和配置页均能读取声望分支。
6. 390px 窄屏下声望分支卡无横向溢出，控制台无错误。

## 2026-07-14 Daily Goal Pass

检查范围：

1. 游戏主界面新增结构化每日目标面板。
2. 面板需要显示目标标题、说明、整体进度、子条件和奖励。
3. 运营后台需要显示目标结算 KPI 和子条件进度。
4. Agent 工作台需要显示每日目标证据、诊断、行动计划和工具轨迹。
5. 配置视图需要显示 `dailyGoals` 配置字段。

自查结论：

1. 目标面板采用“账本目标牌”形式，不遮挡 NPC、经营抉择、关系任务和成就卡。
2. 子条件使用紧凑列表展示，接待、收入、满意度、销量和声望都能在收摊前被玩家理解。
3. 运营页目标行补充标题、奖励和子条件，适合面试时解释目标漏斗。
4. Agent 页新增每日目标压力诊断和奖励校准建议，和工具轨迹保持一致。
5. 浏览器实测中，目标初始进度、接待后进度、收摊事件、运营页、Agent 页和配置页均正常。
6. 390px 窄屏下目标面板无横向溢出，控制台无错误。

## 2026-07-14 Event Choice Pass

检查范围：

1. 游戏主界面新增经营抉择卡。
2. 每个事件需要显示两种抉择按钮，并在选择后切换为已生效状态。
3. 运营后台需要显示抉择次数，Agent 工作台需要显示经营抉择诊断和行动计划。
4. 配置视图需要显示 eventChoices 配置字段。

自查结论：

1. 抉择按钮采用标题、描述和效果三段式，不需要玩家阅读外部说明。
2. 选择后卡片切换为 resolved 状态，避免玩家误以为可以反复选择。
3. 抉择卡位于 NPC 记忆和关系任务之间，不遮挡接待、收摊、补货等核心操作。
4. 运营后台和 Agent 工作台继续保持工具型布局，仅新增必要指标与诊断信息。
5. 浏览器实测中，选择“点亮引路灯”后预算行显示为 16，运营页和 Agent 页均能读取本次抉择。
6. 390px 窄屏下选择前后均无横向溢出，控制台无错误。

## 2026-07-14 Event Illustration Pass

检查范围：

1. 游戏主界面的怪谈事件卡新增事件插图。
2. 事件插图需要随 `activeMysteryEvent` 切换。
3. 桌面和窄屏下，插图不能挤压事件标题、描述、规则效果和主要操作按钮。

自查结论：

1. 桌面端事件卡采用插图 + 文案双列结构，规则说明仍保持可读。
2. 620px 以下事件卡切换为单列，插图位于文字上方，减少横向挤压。
3. 插图作为事件卡内容展示，不会遮挡夜市场景中的商品库存和玩家操作区。
4. 事件插图资产已记录到 `docs/AssetPromptLog.md`，后续可替换为 image-2 PNG。

## 2026-07-14 Achievement Feedback Pass

检查范围：

1. 游戏主界面新增“夜市成就”卡，展示 4 个成就的进度、完成状态和解锁日期。
2. 运营后台新增成就解锁 KPI，事件埋点列表需要显示 `achievement` 事件。
3. Agent 工作台新增“成长反馈”诊断和“放大成就反馈”行动计划。
4. 配置视图新增 achievements 字段，说明成就配置和运营指标用途。

自查结论：

1. 初始状态下成就卡可见，未解锁成就显示百分比进度。
2. 首次成交后“第一盏灯亮”显示 Day 1 解锁，消息框同步提示成就解锁。
3. 第 1 天低流失收摊后“稳摊一夜”显示 Day 1 解锁，已修正收摊后日期误读问题。
4. 运营页显示成就解锁率，Agent 页显示成就证据、成长反馈诊断和行动计划。
5. 桌面视口下图片无断裂、控制台无错误，当前页面未发现横向溢出。

## 2026-07-14 Relationship Quest Pass

检查范围：

1. 游戏主界面新增关系任务卡，包含标题、说明、进度条、奖励和完成态。
2. 库存商品卡需要在任务完成后正确显示纸灯笼补货折扣。
3. 运营后台需要在 KPI 区显示关系进度，Agent 工作台需要展示关系任务读取和建议。

自查结论：

1. 关系任务卡在桌面视口中不会遮挡 NPC 信息、操作按钮和商品库存。
2. 任务从 0/2 推进到完成后，文案切换为“已完成：纸灯笼补货成本 -2”，反馈明确。
3. 纸灯笼补货按钮从 `补货 5` 变为 `补货 3`，玩法奖励可见。
4. 运营后台显示关系进度 100%，Agent 工作台显示 `read_relationship_quest` 工具轨迹和“推进灯纸匠关系线”行动计划。
5. 浏览器控制台未发现错误；当前浏览器检查未发现横向溢出。

## 2026-07-14 Gameplay Art Pass

检查范围：

1. 游戏主界面新增夜市背景、事件卡、商品图标和 NPC 头像。
2. 商品卡新增描述、稀有度边框、图标和有效魅力显示。
3. 顶部和游戏区色彩调整为深色夜市背景配暖色灯光。

自查结论：

1. 桌面视口下 11 张图片资源均正常加载，无断裂图片。
2. `.night-scene` 背景已通过 CSS 资源引用正常显示。
3. 事件卡文字可读，没有遮挡主要操作按钮。
4. 390px 视口下没有横向溢出，商品卡数量和事件卡仍可访问。
5. 运营后台和 Agent 工作台沿用清晰工具型布局，避免被游戏装饰过度干扰。

## 2026-07-13 Portfolio Demo

检查范围：

1. README 截图覆盖游戏、运营后台、Agent 工作台和配置工作台。
2. 演示脚本按 10 分钟节奏组织，避免面试展示时迷路。
3. 面试讲稿把“AI Agent 工程价值”放在安全边界、工具调用和配置治理上，而不是只描述界面。

自查结论：

1. 截图来自本地实际运行页面，可以作为真实功能证据。
2. README 首屏能在短时间内说明项目不是普通小游戏，而是 LiveOps + Agent 闭环。
3. 后续如果新增真实模型调用，需要补充模型输出截图和失败兜底状态。

## 2026-07-13 Tauri Desktop

检查范围：

1. 桌面端默认窗口尺寸为 1280 x 840，适合展示当前四个主视图。
2. 最小窗口尺寸为 1024 x 720，避免核心面板被压缩到不可读。
3. 桌面端复用 Web UI，不新增单独的壳层导航，减少视觉割裂。

自查结论：

1. 桌面端配置优先保证演示稳定性，而不是追求复杂原生菜单。
2. 后续如加入 SQLite、日志导出或本地文件能力，需要重新检查权限提示、空状态和错误状态。

## 2026-07-13 Config Hot Update

已检查：

1. 配置页新增配置热更新工作台、schema 校验、当前生效配置、AI 草案和配置历史。
2. 应用草案和回滚配置按钮具备清晰位置和状态。
3. 配置历史使用状态标签，能区分 applied 和 rolled_back。
4. 390px 窄屏视口下配置摘要、校验、当前配置、草案、历史和 JSON 未发现内容溢出。
5. 浏览器控制台未发现错误。

待继续检查：

1. 后续接入真实 schema 库后，需要展示字段级错误定位。
2. 后续配置历史增多后，需要增加筛选和折叠。

## 2026-07-13 AI Ops Agent

已检查：

1. Agent 工作台新增异常与原因假设、行动计划、数值调参建议和人工确认清单。
2. 异常卡片使用 severity 标识，桌面视口下可快速扫描。
3. 调参建议以 before -> after 的形式展示，适合面试讲解。
4. 390px 窄屏视口下 Agent 新增模块、配置草案和主要按钮未发现内容溢出。
5. 浏览器控制台未发现错误。

待继续检查：

1. 后续接入真实模型后，需要检查长文本、失败重试、加载状态和模型错误提示。
2. 后续接入配置 schema 后，需要展示校验失败状态。

## 2026-07-13 Ops Dashboard

已检查：

1. 运营后台新增任务完成、商品转化、NPC 互动、流失节点和 A/B 测试模块。
2. 桌面视口下运营后台采用双列后台布局，核心 KPI 和趋势图横跨全宽。
3. KPI 使用响应式网格，数量增加后仍保持稳定间距。
4. 390px 窄屏视口下运营表格、任务进度、流失节点和 A/B 卡片未发现内容溢出。
5. 浏览器控制台未发现错误。

待继续检查：

1. 后续接入真实图表库后，需要检查图表标签密度和 tooltip 可读性。
2. 后续接入真实实验数据后，需要增加空状态、加载状态和异常状态。

## 2026-07-13 Agent NPC

已检查：

1. 游戏主界面新增 NPC 记忆卡，桌面视口下与顾客信息、消息框和操作按钮保持清晰层级。
2. Agent 页新增最近 NPC 决策卡和 NPC 记忆账本。
3. 390px 窄屏视口下 NPC 记忆卡、Agent 决策卡、记忆账本和主要按钮未发现内容溢出。
4. Agent 页工具轨迹和规则边界可读，适合面试讲解。
5. 浏览器控制台未发现错误。

待继续检查：

1. 后续接入真实模型输出后，需要检查长文本对卡片高度和滚动体验的影响。
2. 后续接入图片素材后，需要检查 NPC 头像、摊位背景和商品图标的风格统一性。

## 2026-07-13 Web MVP

已检查：

1. 桌面宽屏布局包含游戏区、经营面板和库存区。
2. 窄屏下主布局、库存、指标卡会折叠为单列或双列。
3. 主要按钮具备禁用状态。
4. 游戏界面和运营后台使用不同信息密度，但保持统一边框、颜色和间距。
5. Agent 输出分为证据、风险、建议、工具轨迹和 JSON 草案。
6. 1280px 桌面视口下主要按钮、指标卡、商品卡和面板未发现内容溢出。
7. 390px 窄屏视口下核心标题、接待按钮和主要控件未发现内容溢出。

待继续检查：

1. 后续接入真实图片后检查风格统一性。
2. 增加加载、失败和空状态。
