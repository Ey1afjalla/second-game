# 开发日志

## 2026-07-14 v1.2.0 Final Desktop Menu Pass

完成内容：
1. Tauri 桌面端新增原生菜单，包含“文件”“视图”“帮助”三组入口。
2. “视图”菜单支持切换游戏、运营、Agent、配置四个主工作区。
3. “文件”菜单支持导出演示快照、重置本地演示、刷新窗口和退出应用。
4. “帮助”菜单支持打开 GitHub 仓库，便于面试现场展示版本管理链路。
5. React 前端新增 `desktop-menu://tab` 和 `desktop-menu://action` 监听，只在 Tauri 环境启用，Web 版继续使用页面内按钮。
6. 配置视图新增 `desktopMenu` 字段，记录菜单事件、支持动作和桌面交付用途。
7. 版本号更新为 `1.2.0`，作为当前作品集最终版。

技术选择：
1. 菜单层只发送意图事件，具体业务动作仍由 React 端已有函数执行，避免 Rust 侧重复实现游戏逻辑。
2. 使用动态导入 `@tauri-apps/api/event`，保证浏览器 Web 版不会因为 Tauri API 不存在而报错。
3. 保留 Web 页面内原有按钮，把原生菜单作为桌面快捷入口，而不是替换主 UI。

问题与解决：
1. 原桌面端只是 Tauri 壳，缺少系统级入口，面试时不容易体现“桌面应用交付”。
2. 新增菜单后，桌面版本可以从系统菜单完成切页、导出、重置、刷新和退出，更接近可交付工具。
3. 菜单事件和本地存档、快照、配置视图联动，补齐最终版交付闭环。

版本状态：
1. 已通过 `npm.cmd run build`。
2. 已通过 `cargo check`。
3. 已通过 `npm.cmd run desktop:build`。
4. 已生成 Windows NSIS 安装包：`src-tauri/target/release/bundle/nsis/AI LiveOps Night Market_1.2.0_x64-setup.exe`。
5. 版本管理对应 `v1.2.0-final-desktop-menu` tag。

## 2026-07-14 v1.1.2 Local Demo Reset Pass

完成内容：

1. 本地存档卡新增“重置本地演示”按钮。
2. 重置会回到干净 Day 1 开局，并覆盖当前自动存档。
3. 重置不会删除已保存的演示槽位，便于面试时在干净开局和预设局面之间切换。
4. Agent 行动计划新增“重置本地演示开局”，调参建议新增 `local_demo_reset`。
5. 配置视图 `localPersistence` 新增 `resetCurrentDemo`，明确重置动作、保留内容和用途。
6. 版本号更新为 `1.1.2`。

技术选择：

1. 复用现有 `makeInitialState` 和自动存档 effect，避免额外引入一套状态恢复逻辑。
2. 重置当前演示和删除槽位分离，降低误删面试预设局面的风险。
3. 按钮放在本地存档卡内部，并使用较温和的警示色，表示这是存档管理动作而不是普通游戏操作。

问题与解决：

1. 只有顶部图标重开时，用户不容易理解它和本地自动存档之间的关系。
2. 新增显式按钮后，演示者可以清楚说明当前自动存档会被覆盖，但槽位会保留。
3. 该能力补齐了 Web/Tauri 本地演示中的“恢复干净现场”流程。

版本状态：

1. 已通过 `npm run build`。
2. 已在 `http://127.0.0.1:5173/` 验证本地存档卡显示“重置本地演示”按钮。
3. 已验证保存槽位后点击重置：游戏回到干净 Day 1，事件恢复为 2 条，页面出现“已重置为干净开局，并覆盖当前自动存档；演示槽位仍然保留。”反馈，槽位仍显示在列表中。
4. 已验证 Agent 页显示“重置本地演示开局”和 `local_demo_reset`。
5. 已验证配置页显示 `resetCurrentDemo`、`resetToInitialStateAndOverwriteCurrentAutoSave`、`saveSlots` 和 `importableSnapshots`。
6. 已尝试 390px viewport 检查，但浏览器插件仍保持桌面宽度；按钮在桌面宽度无横向溢出，移动端依赖现有单列布局和按钮最小宽度约束。
7. 待提交并推送到 GitHub。

## 2026-07-14 v1.1.1 Ops Log Export Pass

完成内容：

1. 运营诊断后台新增“运营日志导出”面板，可下载本地 JSON 证据包。
2. 证据包包含局面摘要、核心指标、Agent 报告、事件流、工具轨迹和配置历史。
3. Agent 行动计划新增“导出运营日志证据包”，调参建议新增 `ops_log_export`。
4. 配置视图 `localPersistence` 新增 `opsLogExport`，记录 schema、包含字段和桌面迁移用途。
5. 版本号更新为 `1.1.1`，作为继续按版本推送 GitHub 的小版本。

技术选择：

1. 先使用浏览器下载 JSON，保持 Web 版和 Tauri 壳共用同一套前端能力。
2. 导出内容和普通演示快照区分：快照用于恢复局面，运营日志用于复盘行为、建议和配置变化。
3. 运营页导出按钮给出即时反馈，方便现场演示确认文件已生成。

问题与解决：

1. 只有可视化看板时，面试官很难判断 Agent 建议是否来自完整事件证据。
2. 新增运营日志后，游戏运营、AI Agent 和工程交付三条线能收束到一个可下载证据包。
3. 该能力先留在本机，不上传玩家数据，符合桌面 Demo 的隐私边界。

版本状态：

1. 已通过 `npm run build`。
2. 已在 `http://127.0.0.1:5173/` 验证运营页显示“运营日志导出”和“导出运营日志”按钮。
3. 已点击导出按钮，页面出现“已生成本地运营日志 JSON。”反馈，控制台无错误。
4. 已验证 Agent 页显示“导出运营日志证据包”和 `ops_log_export`。
5. 已验证配置页显示 `opsLogExport` 和 `second-game-ops-log`。
6. 浏览器插件 viewport 覆盖未实际切换到 390px，已通过响应式 CSS 检查确认导出面板在 `max-width: 980px` 下纵向排列且按钮宽度为 100%。
7. 待提交并推送到 GitHub。

## 2026-07-14 Local Stage 6/7 Multi Save Slots Pass

完成内容：

1. 新增本地“演示槽位”能力，最多保存 3 个局面。
2. 每个槽位记录标题、保存时间、经营日、接待数、成交数、铜钱和完整 `GameState`。
3. 本地存档卡新增“保存当前局面”“读取”“删除”操作，支持在多个演示局面之间切换。
4. 槽位数据使用独立 `localStorage` key：`second-game.local-save-slots.v1`，不影响当前自动存档。
5. Agent 行动计划新增“维护多组演示槽位”，调参建议新增 `demo_save_slots`。
6. 配置视图 `localPersistence` 新增 `saveSlots`，说明槽位 key、最大数量和支持动作。

技术选择：

1. 槽位先限制为 3 个，避免 UI 变成完整存档管理器，保持面试演示轻量。
2. 读取槽位复用 `setGame` 和自动存档 effect，保证读取后刷新页面仍保留该局面。
3. 删除槽位只影响槽位列表，不删除当前自动存档，避免误操作破坏正在演示的局面。

问题与解决：

1. 只有一个最新存档时，面试中想展示高热度、关系任务、配置回滚等不同局面需要现场重玩。
2. 多槽位让项目从“能恢复最新状态”升级为“能准备多个演示场景”。
3. 槽位列表放在本地存档卡内部，明确它属于本机演示能力，不涉及上传或账号体系。

版本状态：

1. 已通过 `npm run build`。
2. 已在 `http://localhost:5173/` 验证：空槽位提示、保存当前局面、读取槽位、删除槽位均正常。
3. 已验证 Agent 页显示“维护多组演示槽位”和 `demo_save_slots`，配置页显示 `saveSlots`、`second-game.local-save-slots.v1`、`maxSlots` 和 `loadSlot`。
4. 已用 390px 宽度检查演示槽位区域，无横向溢出；控制台无错误。
5. 本阶段按用户要求仅做本地修改，不提交、不推送 GitHub。

## 2026-07-14 Local Stage 6/7 Snapshot Import Pass

完成内容：

1. 新增“导入演示快照”能力，支持选择本项目导出的 JSON 快照恢复游戏进度。
2. 新增 `DemoSnapshotPayload` 和 `readImportedGameSnapshot`，对 schema、版本号和最小 `GameState` 结构做校验。
3. 导入成功后会恢复局内状态，并通过本地自动存档立即写回 `localStorage`。
4. 本地存档卡新增导入按钮，和“导出演示快照”形成导出、复现、继续演示的闭环。
5. Agent 行动计划新增“导入演示快照复现局面”，调参建议 `local_demo_snapshot` 更新为导出和导入都覆盖。
6. 配置视图 `localPersistence` 新增 `imports` 和 `importValidation` 字段，说明可导入格式和校验边界。

技术选择：

1. 导入暂时只接受 JSON 文件，不接外部服务，保证 Web 和 Tauri 桌面壳都能复用。
2. 导入校验只认当前版本 schema 和本地存档 payload，避免把任意 JSON 当作游戏状态。
3. 导入后不绕过存档链路，而是复用 `setGame` 和自动存档 effect，确保刷新后仍能恢复导入状态。

问题与解决：

1. 上一版只能导出复盘证据，但不能从快照重新进入特定局面。
2. 新增导入后，面试前可以准备一份高价值局面快照，现场稳定复现商品组合、怪谈事件和 Agent 建议。
3. 文件导入失败时会在本地存档卡显示错误，不会破坏当前局内状态。

版本状态：

1. 已通过 `npm run build`。
2. 已在 `http://localhost:5173/` 验证：游戏页显示“导入演示快照”和“导出演示快照”，文件输入限制为 `application/json,.json`，本地存档卡无横向溢出。
3. 已验证 Agent 页显示“导入演示快照复现局面”和 `local_autosave_json_export_and_import`，配置页显示 `localPersistence.imports`、`importValidation` 和 `second-game-demo-snapshot`。
4. 已用 390px 宽度检查本地存档卡、导入按钮和导出按钮，无横向溢出；控制台无错误。
5. 当前浏览器自动化接口不提供本地文件选择器上传能力，因此未自动化执行真实 JSON 文件选择；导入解析、版本校验和状态恢复路径已通过 TypeScript 构建校验。
6. 本阶段按用户要求仅做本地修改，不提交、不推送 GitHub。

## 2026-07-14 Local Stage 6/7 Local Persistence Pass

完成内容：

1. 新增本地自动存档能力，使用 `localStorage` 保存当前游戏进度。
2. 页面刷新或桌面壳重新打开时，会优先读取本地存档恢复游戏状态。
3. 新增本地存档状态卡，展示自动存档状态、事件数量、配置历史数量。
4. 新增“导出演示快照”按钮，可导出包含游戏状态、事件流、关键指标、Agent 报告和配置草案的 JSON。
5. Agent 行动计划新增“导出演示数据快照”，调参建议新增 `local_demo_snapshot`。
6. 配置视图新增 `localPersistence` 字段，说明本地缓存 key、版本、导出内容和后续迁移路径。
7. 本地存档带版本号和最小校验，坏存档会被清理，避免页面启动失败。

技术选择：

1. 先使用 `localStorage`，因为 Web 版和 Tauri 桌面壳都能复用，改动小且稳定。
2. 快照导出使用浏览器 Blob 下载，不需要后端服务。
3. 快照中只包含游戏演示数据、指标和 Agent 报告，不包含账号密码、Token 或外部凭据。

问题与解决：

1. 需求中桌面端需要本地缓存或本地数据能力，原版本刷新后会丢失局内演示状态。
2. 新增自动存档后，面试演示中可以保留经营进度。
3. 新增 JSON 快照后，可以把事件流和 Agent 输出作为复盘证据导出。
4. 当前版本先不接 SQLite，避免在前端稳定阶段引入额外桌面权限和迁移复杂度。

版本状态：

1. 已通过 `npm run build`。
2. 已在 `http://localhost:5173/` 验证：游戏页显示本地存档卡和“导出演示快照”按钮；接待后刷新页面能恢复成交后的金币、好感和成就状态。
3. 已验证 Agent 页显示“导出演示数据快照”和 `local_demo_snapshot`，配置页显示 `localPersistence`、`localStorage`、存档 key 和快照导出字段。
4. 已用 390px 宽度检查本地存档卡与导出按钮，无横向溢出；控制台无错误。
5. 点击导出按钮后页面反馈“已生成本地演示快照 JSON”，导出实现已加固为插入 DOM 链接后触发浏览器下载。
6. 本阶段按用户要求仅做本地修改，不提交、不推送 GitHub。

## 2026-07-14 Local Stage 8 Rumor Heat Pass

完成内容：

1. 新增“怪谈热度”系统，把商品组合的风险和事件倾向显性化。
2. 商品组合新增 `riskWeight` 和 `eventBias` 字段，分别影响热度评分和下一日事件倾向。
3. 暖灯茶点、巡夜口粮会降低风险或偏向灯火类事件；灯下面具传闻会提高热度并强化神秘订单倾向。
4. 收摊时不再只做固定事件轮转，而是读取当前库存、组合、稀有货、声望和满意度生成下一日事件倾向。
5. 新增 `rumor_heat` 事件埋点，记录每日收摊时的热度快照。
6. 游戏主界面新增“怪谈热度”卡，展示热度、状态说明、进度条和下一日事件倾向。
7. 高热度会让狐面少年更容易提前到访，形成“高风险高价值”的经营压力。
8. NPC Agent 工具轨迹新增 `read_rumor_forecast`，用于解释推荐和风险。
9. 运营后台新增“怪谈热度” KPI，并在商品组合策略面板展示下一事件倾向和热度快照次数。
10. 配置视图新增 `rumorForecast` 字段，输出热度输入、事件评分和配置化目的。

技术选择：

1. 使用确定性评分替代随机概率，保证面试演示可复现。
2. 使用 `getRumorForecast` 派生状态，避免新增持久化字段造成回滚和配置复杂度。
3. 事件倾向同时读取商品组合和经营状态，让玩法、埋点、运营看板和 Agent 输入保持一致。

问题与解决：

1. 商品组合此前只影响成交奖励，尚未体现需求中的“风险”和“事件触发概率”。
2. 新增热度后，玩家能看到组合为什么会影响下一日事件，运营也能看到热度快照。
3. 为避免黑盒随机，当前版本显示的是“事件倾向评分”，后续可以在配置层接入真实随机或权重抽取。

版本状态：

1. 已通过 `npm run build`。
2. 已在 `http://localhost:5173/` 验证：游戏页显示“怪谈热度”和组合热度增减，收摊后消息显示热度与下一日事件倾向，运营页显示“怪谈热度” KPI、热度快照和 `rumor_heat` 事件，Agent 页显示 `read_rumor_forecast`、行动计划和调参建议，配置页显示 `rumorForecast`、`riskWeight` 和 `eventBias`。
3. 已用 390px 宽度检查怪谈热度卡，收摊前后无横向溢出，控制台无错误。
4. 本阶段按用户要求仅做本地修改，不提交、不推送 GitHub。

## 2026-07-14 Local Stage 8 Product Combo Pass

完成内容：

1. 新增“商品组合”系统，把库存从单件补货升级为成套经营策略。
2. 新增 3 组组合：暖灯茶点、巡夜口粮、灯下面具传闻。
3. 组合会读取真实库存，只有成套商品都有余量时才激活。
4. 激活组合会给对应商品增加魅力，并在成交时提供额外满意、声望或铜钱奖励。
5. 游戏主界面新增商品组合卡，显示激活数量、组合说明、缺少商品和实际效果。
6. NPC Agent 在接待时读取 `read_product_combos`，商品推荐会纳入组合魅力。
7. 新增 `product_combo` 事件埋点，用于记录组合奖励触发。
8. 运营后台新增“组合激活” KPI 和“商品组合策略”面板，展示激活率与触发次数。
9. Agent 报告新增商品组合证据、异常诊断、行动计划和调参建议。
10. 配置视图新增 `productCombos` 字段，说明组合规则可热更新和可配置化。

技术选择：

1. 使用 `getActiveProductCombos` 按库存派生组合状态，避免新增冗余状态。
2. 使用 `getProductComboCharmBonus` 和 `getProductComboSaleBonus` 分离展示魅力与成交奖励。
3. 组合奖励进入事件流、运营看板和 Agent 报告，保证玩法不是只停留在 UI 文案。

问题与解决：

1. 原商品系统主要是价格、成本和魅力，玩家的补货策略比较线性。
2. 新增组合后，玩家需要思考“哪些商品应该一起保留库存”，游戏性更接近经营策略。
3. 为避免数值膨胀，组合奖励只绑定少数商品，并分别影响满意、声望或收入。

版本状态：

1. 已通过 `npm run build`。
2. 已在 `http://localhost:5173/` 验证：游戏页显示“商品组合”和 3 组激活，接待后触发“暖灯茶点”组合奖励，运营页显示组合 KPI、策略面板和 `product_combo` 事件，Agent 页显示 `read_product_combos`、行动计划和调参建议，配置页显示 `productCombos`。
3. 已用 390px 宽度检查商品组合卡，接待前后无横向溢出，控制台无错误。
4. 本阶段按用户要求仅做本地修改，不提交、不推送 GitHub。

## 2026-07-14 Local Stage 8 Reputation Branch Pass

完成内容：

1. 新增“声望分支”系统，把声望从静态数值升级为会影响经营规则的分支。
2. 声望低于等于 3 时触发“巡夜调查”：顾客预算 -1，未成交额外声望 -1。
3. 声望达到 12 时触发“灯影贵客”：狐面少年更容易到访，稀有商品魅力 +1。
4. 游戏主界面新增声望分支卡，显示当前分支、进度、说明和实际效果。
5. NPC 到访选择、预算计算、商品魅力计算和未成交惩罚都会读取声望分支。
6. 新增 `reputation_branch` 事件埋点，用于记录非稳定分支触发。
7. 运营后台新增“声望分支” KPI。
8. Agent 报告新增声望分支证据、异常诊断、行动计划和调参建议。
9. Agent 工具轨迹新增 `read_reputation_branch`，NPC 决策也会展示声望分支输入。
10. 配置视图新增 `reputationBranches` 字段，说明声望分支可配置化。

技术选择：

1. 使用 `getReputationBranch` 派生当前分支，避免新增冗余状态。
2. 使用 `getNpcForReputation` 影响下一位顾客，让高低声望改变到访结构。
3. 分支效果进入预算、魅力和惩罚函数，保证不是只做 UI 提示。

问题与解决：

1. 阶段 8 中“声望分支”此前只停留在需求文档，没有实际规则。
2. 新增分支后，玩家可以看到声望门槛、当前状态和下一步风险/收益。
3. 为避免首局过度惩罚，中间状态保持“街坊观望”，只提示高低门槛。

版本状态：

1. 已通过 `npm run build`。
2. 已在 `http://localhost:5173/` 验证：游戏页显示“街坊观望”，接待后 Agent 工具轨迹读取 `read_reputation_branch`，运营页显示“声望分支” KPI，配置页显示 `reputationBranches`。
3. 已用 390px 宽度检查声望分支卡，接待前后无横向溢出，控制台无错误。
4. 本阶段按用户要求仅做本地修改，不提交、不推送 GitHub。

## 2026-07-14 Local Stage 8 Daily Goal Pass

完成内容：

1. 将原本的 `dayGoals` 文案升级为结构化 Daily Goal 系统。
2. 新增 3 个每日目标：雾夜开张、稳住街坊、试营业收官。
3. 每日目标包含标题、描述、奖励和子条件进度，例如接待人数、收入、满意度、销量和声望。
4. 游戏主界面新增目标进度面板，展示百分比、子条件和达成奖励。
5. 收摊时根据真实目标条件结算，达成写入奖励，未达成扣减满意度。
6. 新增 `daily_goal` 事件埋点，记录每日目标达成或未达成。
7. 运营后台新增“目标结算” KPI，并在任务完成率中展示目标标题、奖励和子条件。
8. Agent 报告新增每日目标证据、每日目标压力诊断、目标奖励校准行动计划和工具轨迹。
9. 配置视图新增 `dailyGoals` 字段，说明每日目标可以作为后续配置化玩法模块。

技术选择：

1. 使用 `buildDailyGoal`、`getDailyGoalProgress`、`isDailyGoalCompleted` 统一计算目标状态，避免 UI、运营后台和收摊逻辑各自判断。
2. 每日目标进入事件流，运营后台和 Agent 继续以事件数据为主要事实来源。
3. 目标奖励仍由规则系统结算，Agent 只能读取目标数据并提出调优建议。

问题与解决：

1. 旧版本只有一句“今日目标”提示，玩家不知道各条件完成到哪一步。
2. 新面板拆出子条件，让短局目标更明确，也让收摊前的风险更可见。
3. 原收摊奖励条件较粗略，已改为按每一天的真实目标条件结算。

版本状态：

1. 已通过 `npm run build`。
2. 已在 `http://localhost:5173/` 验证：目标初始进度、接待后进度、收摊后的 `daily_goal` 事件、运营页 KPI、Agent 证据和配置页 `dailyGoals` 均正常。
3. 已用 390px 宽度检查目标面板，接待前后无横向溢出，控制台无错误。
4. 本阶段按用户要求仅做本地修改，不提交、不推送 GitHub。

## 2026-07-14 Local Stage 8 Event Choice Pass

完成内容：

1. 新增“经营抉择”系统，让玩家每天可以主动处理当前怪谈事件。
2. 夜雾压街新增“点亮引路灯 / 省下灯油”两种选择，影响预算惩罚、铜钱和满意度。
3. 纸灯将熄新增“换新灯芯 / 借暗卖面”两种选择，影响商品魅力和声望。
4. 神秘订单新增“接下神秘订单 / 婉拒无名订单”两种选择，影响订金、打赏、声望和失败惩罚。
5. 新增 `eventChoices` 状态和 `event_choice` 事件埋点。
6. NPC 接待逻辑、商品有效魅力、神秘订单打赏和未成交惩罚都会读取当日抉择。
7. 游戏主界面新增经营抉择卡，选择后显示本日已生效规则。
8. 运营后台新增“抉择次数” KPI，Agent 报告新增“经营抉择”异常检查、证据和行动计划。
9. 配置视图新增 eventChoices 字段，说明抉择可作为后续配置化玩法模块。

技术选择：

1. 抉择记录挂在 `GameState.eventChoices`，并通过 `day + eventId` 限定每天每个事件只能选择一次。
2. 抉择不直接绕过规则系统，而是作为参数传入预算、魅力和打赏计算函数。
3. Agent 工具轨迹新增 `read_event_choice`，让最近一次 NPC 决策可以解释当前选择如何影响推荐结果。

问题与解决：

1. 事件原先只由系统轮转，玩家缺少主动选择。
2. 新增抉择后，每个事件都拥有“保守 / 冒险”或“投入 / 节省”的分叉。
3. 抉择按钮文案较长，已使用网格卡片和小字号说明，避免影响接待、收摊和补货按钮。

版本状态：

1. 已通过 `npm run build`。
2. 已在 `http://localhost:5173/` 验证：点击“点亮引路灯”后铜钱扣除、预算恢复到 16、运营页写入 `event_choice`、Agent 页读取经营抉择并生成建议。
3. 已用 390px 宽度检查经营抉择卡，选择前后无横向溢出，控制台无错误。
4. 本阶段按用户要求仅做本地修改，不提交、不推送 GitHub。

## 2026-07-14 Local Stage 8 Event Illustration Pass

完成内容：

1. 根据阶段 8 图片资产清单，新增 3 张怪谈事件插图：夜雾压街、纸灯将熄、神秘订单。
2. 扩展 `MysteryEvent` 数据结构，新增 `illustration` 字段。
3. 游戏主界面的怪谈事件卡从纯文本升级为“事件插图 + 规则说明”结构。
4. 事件插图跟随当前 `activeMysteryEvent` 切换，和预算、魅力、打赏等规则变化保持一致。
5. 补充窄屏样式，让事件插图在小屏下改为单列展示，避免压缩文字。
6. 在 `docs/AssetPromptLog.md` 中记录事件插图用途、提示词、反向提示词和是否进入项目。

技术选择：

1. 当前继续使用本地 SVG 资产，保证项目离线可运行和构建稳定。
2. SVG 文件名沿用需求文档里的事件用途，后续可逐步替换为 image-2 生成的 PNG。
3. 插图挂在事件配置上，而不是写死在 UI 中，方便后续把事件配置迁移到 JSON 或后端。

问题与解决：

1. 事件卡原本宽度较窄，直接加入图片会挤压规则文案。
2. 桌面端改为 92px 插图 + 文案双列，小屏端切换为单列。
3. 图片与文字同属事件卡，不会遮挡背景、库存商品和主要操作按钮。

版本状态：

1. 已通过 `npm run build`。
2. 本阶段按用户要求仅做本地修改，不提交、不推送 GitHub。

## 2026-07-14 Local Stage 8 Achievement Feedback Pass

完成内容：

1. 在阶段 8 玩法增强基础上新增“夜市成就”系统，补强短局成长反馈。
2. 新增 4 个成就：第一盏灯亮、旧灯约兑现、回头客信任、稳摊一夜。
3. 成就会根据成交、关系任务、NPC 好感和每日低流失经营结果自动解锁。
4. 游戏主界面新增成就卡，显示已解锁状态、解锁日期和未解锁进度。
5. 成就解锁写入 `achievement` 事件，进入统一事件流。
6. 运营后台新增“成就解锁”KPI，并能在事件埋点列表看到成就事件。
7. Agent 报告新增“成长反馈”异常检查、证据文案和“放大成就反馈”行动计划。
8. 配置视图新增 achievements 字段，说明成就作为可配置玩法目标和运营漏斗指标。

技术选择：

1. 成就系统复用前端状态和事件流，不新增独立数据源，保持运营后台和 Agent 数据一致。
2. 成就触发集中在 `evaluateAchievements`，由成交和收摊后的状态统一判定，避免 UI 组件里写业务规则。
3. “稳摊一夜”按真实经营日写入解锁日期，避免收摊后切到下一天造成日期误读。

问题与解决：

1. 初次验证时，“稳摊一夜”在第 1 天达成但显示 Day 2。
2. 修正为从 `day_end` 事件反推真实达成日，并用该日期写入成就事件。
3. 成就文案较多，已用紧凑卡片布局控制宽度，避免压住接待、收摊和库存操作。

版本状态：

1. 已通过 `npm run build`。
2. 已完成浏览器验证：首单成就即时解锁、收摊成就写入真实达成日、运营 KPI 读取成就解锁率、Agent 读取成就证据和行动计划、配置 JSON 包含 achievements、控制台无错误。
3. 本阶段按用户要求仅做本地修改，不提交、不推送 GitHub。

## 2026-07-14 Local Stage 8 Relationship Quest Pass

完成内容：

1. 在“游戏性与主题美术增强”阶段中补齐 NPC 关系线要求，新增“灯纸匠的旧灯约”轻任务。
2. 任务目标为向灯纸匠成功售出 2 次纸灯笼，完成后解锁纸灯笼补货成本 -2 的经营奖励。
3. 游戏主界面新增关系任务卡，展示任务说明、进度条、奖励和完成状态。
4. 接待逻辑新增关系任务读取、进度推进、完成判定、奖励生效和 `relationship_quest` 事件写入。
5. 补货成本现在同时读取 LiveOps 配置和关系任务奖励，保证剧情进度能真实改变经营循环。
6. 运营后台新增关系进度 KPI，便于从埋点角度观察剧情玩法是否被玩家推进。
7. Agent 报告新增关系任务证据、异常判断、行动计划和工具调用轨迹 `read_relationship_quest`。
8. 配置视图新增 relationshipQuest 字段，记录关系任务如何读取 NPC 记忆、销售事件和商品偏好。

技术选择：

1. 关系任务先做成轻量状态机，避免引入复杂任务系统，同时保留后续扩展多个 NPC 支线的空间。
2. 奖励选择补货成本折扣，而不是一次性金币，目的是让剧情线持续影响核心经营策略。
3. 任务进度写入事件流并被运营后台和 Agent 同时读取，强化“玩法 - 数据 - Agent 建议”的闭环。

问题与解决：

1. 关系任务需要避免只停留在 UI 展示，所以完成奖励接入 `getRestockCost`，直接影响纸灯笼补货按钮价格。
2. 初次浏览器验证时，灯纸匠第一次任务推进后库存被消耗，需要按正常玩法补货后继续验证第二次完成。
3. Agent 证据文案在任务完成后会切换为完成态描述，因此验证重点改为任务状态、行动计划和工具轨迹是否可读。

版本状态：

1. 已通过 `npm run build`。
2. 已完成浏览器验证：关系任务从 0/2 推进到完成、纸灯笼补货折扣生效、运营后台显示关系进度 100%、Agent 读取 `read_relationship_quest`、控制台无错误。
3. 本阶段按用户要求仅做本地修改，不提交、不推送 GitHub。

## 2026-07-14 Local Stage 8 Gameplay Art Pass

完成内容：

1. 根据 `RequirementsDoc.md` 新增的阶段 8 要求，开始本地开发“游戏性与主题美术增强”版本。
2. 新增本地 SVG 主题资产：夜市背景、5 个商品图标、3 个 NPC 头像。
3. 商品模型新增图标、稀有度和描述文案。
4. NPC 模型新增头像图片。
5. 新增 3 种怪谈事件：夜雾压街、纸灯将熄、神秘订单。
6. 怪谈事件会影响实际规则：预算、商品魅力、额外打赏和满意度。
7. 游戏主界面从纯 CSS 摊位升级为主题背景、事件卡、商品图标和 NPC 头像。
8. 事件切换会写入事件流，后续运营后台和 Agent 可以继续读取。

技术选择：

1. 本轮不上传 GitHub，仅在本地开发和验证。
2. 先用项目内 SVG 资产保证界面稳定可运行，再逐步替换为 image-2 生成的精细位图。
3. 玩法增强优先选择能被埋点和 Agent 读取的规则变化，而不是只做视觉装饰。

问题与解决：

1. 初次使用运行时动态背景路径时，浏览器里 `.night-scene` 背景没有挂上。
2. 改为在 CSS 中直接引用 `./assets/night-market-bg.svg` 后，Vite 可以稳定处理背景资源。
3. 主题化后移动端存在潜在溢出风险，已用 390px 视口检查，未发现横向溢出。

版本状态：

1. 已通过 `npm run build`。
2. 已完成浏览器验证：图片无断裂、事件卡展示、接待规则受事件影响、每日事件切换、390px 窄屏无横向溢出。
3. 本阶段按用户要求不提交、不推送 GitHub。

## 2026-07-13 v1.0.0 Portfolio Demo

完成内容：

1. 将项目版本推进到 `1.0.0`，作为可投递作品集版本。
2. 重写 README 顶部结构，新增 3 分钟亮点、核心闭环、截图和演示材料入口。
3. 新增 `docs/DemoScript.md`，提供 10 分钟演示流程。
4. 新增 `docs/InterviewGuide.md`，提供面试讲解材料、技术亮点和常见追问回答。
5. 新增关键界面截图，覆盖游戏主界面、运营后台、Agent 工作台和配置工作台。
6. 同步更新桌面端版本号，保证 Tauri 安装包版本与作品集版本一致。

技术选择：

1. 作品集打磨阶段不重构核心代码，优先把现有功能转化为清晰、可验证、可讲述的交付物。
2. README 优先服务面试官快速理解，详细过程放入 docs。
3. 截图直接来自本地运行页面，避免使用静态设计稿冒充真实功能。

问题与解决：

1. 面试官阅读时间有限，因此 README 顶部增加“一句话闭环”和 3 个面试重点。
2. AI Agent 工程价值容易被误解为文案生成，因此面试讲稿强调输入、工具、输出、安全边界和配置治理。

版本状态：

1. 已生成四张关键界面截图。
2. 已通过 `npm run build`。
3. 已通过 `cargo check`。
4. 已通过 `npm run desktop:build`，生成 `AI LiveOps Night Market_1.0.0_x64-setup.exe`。
5. 已短暂启动 `second-game.exe`，确认桌面程序进程可以正常拉起。
6. 本版本完成后需要推送到 GitHub。

## 2026-07-13 v0.6.0 Tauri Desktop

完成内容：

1. 新增 Tauri 2 桌面应用工程目录 `src-tauri`。
2. 新增桌面端启动、构建和环境信息脚本。
3. 将桌面端窗口配置为 1280 x 840，最小尺寸为 1024 x 720，适合面试演示时同时展示游戏、后台和 Agent 工作台。
4. 设置桌面应用标识 `com.eyjafjalla.secondgame`。
5. 将 Tauri 应用版本与前端版本同步为 `0.6.0`。
6. 将桌面构建目标收敛到 Windows NSIS 安装包，避免作品集阶段生成过多无关产物。

技术选择：

1. 桌面端复用已有 Web 应用，不重写 UI，保证 Web 与桌面展示内容一致。
2. 使用 Tauri 的 `beforeDevCommand` 和 `beforeBuildCommand` 串联 Vite 开发与生产构建流程。
3. 先完成可运行、可打包的桌面壳，再逐步迁移本地配置历史到 SQLite。

问题与解决：

1. 默认 Tauri 初始化配置使用模板包名和小窗口，已改为作品集项目自己的产品名、应用标识和演示尺寸。
2. 桌面端不额外引入后端逻辑，避免在封装阶段破坏已经验证过的 Web 功能闭环。

版本状态：

1. 已通过 `npm run build`。
2. 已通过 `cargo check`。
3. 已通过 `npm run desktop:build`，生成 Windows NSIS 安装包。
4. 已短暂启动 `second-game.exe`，确认桌面程序进程可以正常拉起。
5. 本版本完成后需要推送到 GitHub。

## 2026-07-13 v0.5.0 Config Hot Update

完成内容：

1. 新增 LiveOps 配置模型，包含版本、状态、活动草案、应用时间和回滚来源。
2. 新增配置校验函数，检查 activityId、target、segment、trigger、guardrail 和 tuning 字段。
3. 游戏状态新增 `activeConfig` 和 `configHistory`。
4. 配置页从只读 JSON 升级为配置热更新工作台。
5. 配置工作台支持应用 AI 活动草案，并记录配置历史。
6. 配置工作台支持从当前活动配置回滚到上一版配置。
7. 应用配置后，首购补贴可以实际影响当前局内金币和收入。
8. 应用配置后，偏好商品补货优先级可以降低对应商品补货成本。

技术选择：

1. 先在前端实现配置生命周期，验证“草案 - 校验 - 人审 - 应用 - 历史 - 回滚”的产品流程。
2. 配置草案继续来自 AI 运营 Agent，配置工作台负责校验与应用边界。
3. 热更新影响范围控制在补贴和补货成本，避免早期版本改动过大。

问题与解决：

1. 回滚需要保留审计记录，因此回滚不是删除历史，而是追加一条 `rolled_back` 记录。
2. 为避免 AI 草案直接污染游戏状态，应用按钮只有在 schema 校验通过后可用。

版本状态：

1. 已通过 `npm run build`。
2. 已完成浏览器验证：配置工作台展示、schema 校验、应用草案、配置历史、回滚、首购补贴影响局内金币、控制台错误检查、390px 窄屏溢出检查。
3. 本版本完成后需要推送到 GitHub。

## 2026-07-13 v0.4.0 AI Ops Agent

完成内容：

1. 扩展 AI 运营 Agent 报告结构，从单段建议升级为结构化运营日报。
2. Agent 现在读取运营后台指标，生成异常与原因假设。
3. Agent 新增行动计划，区分运营配置、LiveOps 和内容策划责任。
4. Agent 新增数值调参建议，包括补货优先级、首购补贴和长尾商品剧情触发。
5. Agent 新增人工确认清单，明确哪些建议进入游戏前需要人审。
6. 活动配置草案升级为 `night-market-retention-002`，包含目标人群、触发条件、预期影响和 tuning 字段。
7. Agent 工作台 UI 新增异常、行动计划、调参建议和人工确认模块。
8. 配置视图增加 `aiOpsAgent` 能力说明。

技术选择：

1. 继续使用本地规则模拟 AI 运营 Agent，先验证工具输入、报告结构和人审边界。
2. 报告直接消费 `buildMetrics` 的结构化指标，保证每条建议可追溯到运营数据。
3. 活动配置草案保持 JSON 结构，为后续 schema 校验和热更新铺路。

问题与解决：

1. Agent 页内容密度继续升高，新增卡片化布局和 severity 标识，保证面试展示时能快速扫描。
2. 调参建议包含字符串和数字，统一转成可展示文本，避免 UI 类型不一致。

版本状态：

1. 已通过 `npm run build`。
2. 已完成浏览器验证：接待顾客后 Agent 页展示日报、异常原因假设、行动计划、调参建议、人工确认清单、配置草案、控制台错误检查、390px 窄屏溢出检查。
3. 本版本完成后需要推送到 GitHub。

## 2026-07-13 v0.3.0 Ops Dashboard

完成内容：

1. 扩展运营指标计算，从事件流中统计任务完成、商品购买转化、NPC 互动、流失节点和 A/B 对比。
2. 运营后台新增任务完成率模块，按三天目标展示进度和达成状态。
3. 运营后台新增商品购买转化表，展示销量、收入、转化率和库存。
4. 运营后台新增 NPC 互动率表，展示到访、好感、情绪和偏好商品。
5. 运营后台新增流失节点诊断，展示进货后未接待、到访未成交、收摊未达标和库存断档。
6. 运营后台新增基础 A/B 测试，对比当前版本和“夜灯补贴”版本的转化、客均收入和留存评分。
7. 配置视图增加 analytics 配置说明，为后续后端埋点和配置化看板做准备。

技术选择：

1. 继续使用事件流作为单一数据来源，让每个运营指标都能追溯到玩家行为。
2. A/B 测试先使用本地模拟版本对比，验证运营后台的信息架构，再接入真实实验分流。
3. UI 采用密集但分区清晰的后台布局，符合游戏运营工具的扫描习惯。

问题与解决：

1. KPI 数量增加后，原四列布局不够稳定，改为响应式 `auto-fit` 网格。
2. 新增表格在窄屏下可能拥挤，移动端统一折叠为单列。

版本状态：

1. 已通过 `npm run build`。
2. 已完成浏览器验证：接待顾客后运营页展示任务完成、商品转化、NPC 互动、流失节点、A/B 对比、控制台错误检查、390px 窄屏溢出检查。
3. 本版本完成后需要推送到 GitHub。

## 2026-07-13 v0.2.0 Agent NPC

完成内容：

1. 为 NPC 增加结构化身份：角色、偏好、预算、厌恶项、记忆钩子。
2. 为每个 NPC 增加动态记忆：好感、到访次数、最近购买、最近反馈、当前情绪。
3. 接待顾客时，NPC Agent 会读取库存、价格、NPC 记忆和任务状态。
4. 购买决策由规则系统确认，Agent 只生成解释、推荐理由和对话反馈。
5. 游戏主界面新增 NPC 记忆卡，展示当前顾客的记忆和好感。
6. Agent 工作台新增最近 NPC 决策、工具调用轨迹和 NPC 记忆账本。
7. 配置视图增加 NPC 记忆字段说明，向后续配置校验和热更新靠拢。

技术选择：

1. 继续使用本地规则模拟 Agent，先验证记忆和工具调用链路。
2. NPC 记忆保存在前端状态中，便于之后迁移到后端或 Tauri 本地 SQLite。
3. 工具轨迹以结构化 `AgentTrace` 保存，方便后续替换为真实模型工具调用日志。

问题与解决：

1. 新增 NPC 记忆后，旧界面仍读取静态情绪字段，已改为读取记忆中的动态情绪。
2. Agent 页信息密度提升后，补充了记忆账本和决策卡片样式，避免文字拥挤。

版本状态：

1. 已通过 `npm run build`。
2. 已完成浏览器验证：首屏 NPC 记忆卡、接待顾客、Agent 决策轨迹、NPC 记忆账本、控制台错误检查、390px 窄屏溢出检查。
3. 本版本完成后需要推送到 GitHub。

## 2026-07-13 v0.1.0 Web MVP

完成内容：

1. 初始化 React + TypeScript + Vite Web 工程。
2. 实现《夜市怪谈摊主》的三天经营 MVP。
3. 实现基础数值：铜钱、声望、满意度、库存、天数。
4. 实现基础循环：补货、接待 NPC、销售、收摊。
5. 实现三类以上 NPC 和五种商品。
6. 实现事件埋点，并在运营看板展示接待、收入、转化和流失。
7. 实现本地模拟 AI 运营日报，输出证据、风险、建议和活动配置草案。
8. 实现配置视图，展示第一版 JSON 配置结构。

技术选择：

1. 第一阶段使用纯前端状态承载游戏和运营数据，降低启动成本，方便面试演示。
2. UI 使用手写 CSS，保证能快速控制视觉层级和响应式效果。
3. 使用 lucide-react 提供工具类图标，提升后台工具感。

问题与解决：

1. PowerShell 脚本策略阻止 `npm` 执行，改用 `npm.cmd`。
2. Vite 交互式初始化因目录非空取消，改为手工创建项目文件。
3. TypeScript 默认解析配置不适配 Vite 7，改为 `moduleResolution: bundler` 并补充 Node 类型。

版本状态：

1. 已通过 `npm run build`。
2. 已完成浏览器验证：首屏渲染、接待顾客、运营看板数据更新、控制台错误检查、390px 窄屏溢出检查。
3. 本版本完成后需要推送到 GitHub。
